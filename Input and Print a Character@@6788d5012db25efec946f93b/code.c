#include <stdio.h>

int main() {
    char character;
    scanf("%c", &character);   // Taking a single character as input
    printf("You entered: %c", character);  // Printing the entered character
    return 0;
}
