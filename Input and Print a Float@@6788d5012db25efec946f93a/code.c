#include <stdio.h>

int main() {
    float number;
    scanf("%f", &number);   // Take input from user
    printf("You entered: %.2f", number);  // Print the float with 2 decimal places
    return 0;
}
