#include <stdio.h>

int main() {
    float num1, num2;
    scanf("%f %f", &num1, &num2);  // Take two floats as input
    printf("Product: %.2f\n", num1 * num2);  // Print the product with 2 decimal places
    return 0;
}
