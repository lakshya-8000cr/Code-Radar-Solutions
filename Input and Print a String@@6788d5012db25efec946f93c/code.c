#include <stdio.h>

int main() {
    char word[100];  // Declaring a character array to store the input word
    scanf("%s", word);   // Taking a single word as input
    printf("You entered: %s", word);  // Printing the entered word
    return 0;
}
